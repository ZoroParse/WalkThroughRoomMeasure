{
  "name": "walkthrough-room-measure",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Turn a 2D room blueprint into an interactive 3D walkthrough for collecting real-world measurements.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
